// TagCategoria — etiqueta com a categoria do produto. [Teste tipo 1: só aparece]
// Props: categoria (string).
export default function TagCategoria({ categoria }) {
  return <span className="tag-categoria">{categoria}</span>;
}
