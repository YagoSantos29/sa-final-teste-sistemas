// Logo — marca da loja, exibida no cabeçalho.
// Sem props. Mostra o nome da loja como um título.

export default function Logo() {
  return (
    <span className="logo">
      PixelPlay
    </span>
  );
}