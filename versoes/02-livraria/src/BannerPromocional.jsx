// BannerPromocional — faixa de destaque com uma mensagem da loja. [Teste tipo 1]
// Props: mensagem (string).
export function BannerPromocional({ mensagem }) {
  return <div className="banner">{mensagem}</div>;
}
